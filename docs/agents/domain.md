# Domain documentation

AutoPair uses a single project context. Read root `CONTEXT.md` when present and
architectural decisions under `docs/adr/` before changing the affected subsystem.
If a change introduces durable domain language or a consequential architectural
choice, update `CONTEXT.md` or add an ADR respectively.
