# Issue tracker

Use GitHub Issues in `ericclemmons/autopair` through the `gh` CLI. Search existing
open and closed issues before creating one, preserve repository templates, and link
implementation pull requests back to their issue when applicable.
